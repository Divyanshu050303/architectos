# Loaded by `next dev` only. The backend API is not implemented yet, so local
# development uses the labelled mock transport. Set to false once apps/api serves /api/v1.
NEXT_PUBLIC_API_MOCKS=true
NEXT_PUBLIC_APP_ENV=development
